METHOD 1:-

Time Complexity:-O(N).
Space Complexity:-O(1).

Approach:-
Traversing all the nodes and getting sum till that node from the root and if sum is equal to target and node has no left and right child than return true. Approach is purely recursive.