METHOD 1:-

Time Complexity:-O(N*32).
Space Complexity:-O(1).
    
Approach:-
    For each bit poisition we will calculate the numbers having 1 at that bit. Now taking it's modulo will give that what is the it of that single nhumber at that bit so we can generate the numeber using that one.